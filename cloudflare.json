{
  "build": {
    "command": "npm run build",
    "output": ".next",
    "environment": {
      "NODE_VERSION": "20"
    }
  },
  "routes": [
    {
      "pattern": "^/api/.*",
      "script": "api"
    }
  ]
}
